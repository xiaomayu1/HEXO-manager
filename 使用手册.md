# Hexo 博客管家 — 使用手册

> 版本 v1.0 · 适用对象：不想敲命令行、只想用图形界面管理 Hexo 博客的写作者
> 随附：Node.js / Hexo CLI / Git 环境的完整安装教程

---

## 一、这是什么

**Hexo 博客管家**（窗口标题 `Hexo 博客管家`、侧栏品牌 `Hexo 工作室`）是一款基于 Electron 的**本地桌面 GUI 工具**，把 Hexo 常用的命令行操作（写文章、预览、换主题、装插件、部署）全部变成点几下鼠标。数据存在本机的 SQLite 库里，不依赖任何云端服务。

核心写作流：**写文章 → 预览 → 部署**，全程不用开终端。

---

## 二、功能总览

左侧导航栏共 7 个页面，登录后还会先经过一次「环境检测」。

| 路由 | 页面 | 能做什么 |
|------|------|----------|
| `dashboard` | 🏠 首页 | 已发布/草稿/待发布统计、最近动态时间线、站点状态、快捷入口 |
| `posts` | 📄 文章 | 新建/编辑/删除/搜索/筛选文章，带 Markdown 编辑器与实时预览，可从 `.txt/.md` 导入 |
| `preview` | 👁️ 本地预览 | 启停 `hexo server`，内嵌 iframe 看博客效果，可一键打开系统浏览器 |
| `themes` | 🎨 主题与插件 | 列出/切换/安装 Hexo 主题，安装/卸载 `hexo-*` 插件 |
| `deploy` | 🚀 部署 | 配置仓库/分支，一键执行 `hexo clean → generate → deploy`，查看部署历史 |
| `settings` | ⚙️ 设置 | 切换亮/暗主题、配置博客路径/标题/网址、环境检测、数据备份与恢复 |
| `profile` | 👤 用户主页 | 换头像、改昵称/简介/用户名、改密码 |

功能与运行环境的依赖关系（**这点很重要**）：

| 功能 | 需要 | 说明 |
|------|------|------|
| 文章管理、仪表盘、设置、用户主页、备份 | 无 | 纯本地数据，装好软件即用 |
| 扫描现有文章 | 已配置博客路径 | 只读取博客目录 `source/_posts/*.md`，不需要 Hexo |
| 安装主题/插件 | Node.js（npm） | 在博客目录里跑 `npm install` |
| 本地预览 | Hexo CLI + 博客路径 | 跑 `hexo server` |
| 部署发布 | Hexo CLI + Git + `hexo-deployer-git` 插件 + `_config.yml` 的 `deploy:` 配置 | 跑 `hexo deploy`，需把改动推到 Git 仓库 |

> 没装齐环境也能进软件、写文章——只是「预览」和「部署」这两项会提示你去装。本手册第三章就是教你把这些一次性装好。

---

## 三、环境安装教程（Node.js / Hexo CLI / Git）

本软件本身是绿色桌面程序，但它调用的 **Hexo** 是建立在三件套之上的：**Node.js**（运行时 + npm）、**Hexo CLI**（博客编译器）、**Git**（部署推送）。下面以 **Windows** 为主链路，macOS / Linux 命令在对应小节给出。

### 3.1 安装 Node.js（必须）

Hexo 需要 Node.js 14 以上，建议直接装 LTS。

1. 打开 https://nodejs.org ，下载 **LTS 版**的 Windows 安装包（`.msi`，如 `node-v20.x.x-x64.msi`）。
2. 双击安装，一路「Next」，确认勾选 **Add to PATH**（默认勾上）。
3. 装完**新开一个 PowerShell**（旧窗口里的 PATH 不会刷新），输入：

   ```powershell
   node -v
   npm -v
   ```

   能分别看到 `v20.x.x` 和 `10.x.x` 这样的版本号即成功。

macOS：`brew install node`；Linux（apt）：`curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash - && sudo apt install -y nodejs`。

### 3.2 安装 Hexo CLI（写文章/预览/部署都靠它）

在 PowerShell 里执行（这是全局安装，装一次永久生效）：

```powershell
npm install -g hexo-cli
```

验证：

```powershell
hexo -v
```

能看到类似 `hexo-cli: 4.3.x` 的输出即可。

> **不想敲命令？** 也可以不装：登录后软件的「环境检测」界面里，只要 Node.js 已装但 Hexo 没装，会出现一个 **「一键安装 Hexo CLI」** 按钮，点它软件会自动跑 `npm install -g hexo-cli` 装好；本地预览页点「启动预览」时若没检测到 Hexo，也会给出同一个一键安装入口。详见第四、六章。

### 3.3 安装 Git（部署发布需要）

1. 打开 https://git-scm.com/downloads ，下载 **Windows** 版安装包。
2. 安装时**一路默认**即可（唯一需要注意的是勾选「Git from the command line and also from 3rd-party software」，默认勾上）。
3. 验证：

   ```powershell
   git --version
   ```

   看到 `git version 2.x.x.windows.x` 即成功。

macOS：首次跑 `git` 会提示安装 Xcode Command Line Tools，同意即可；Linux：`sudo apt install -y git`（apt）/ `sudo yum install -y git`（yum）。

### 3.4 一次性验证三件套

软件内置的「设置 → 环境检测」其实就是帮你跑这三条，你也可以自己在 PowerShell 验：

```powershell
node -v
hexo -v
git --version
```

三条都有版本号 → 全绿，软件所有功能可用。

### 3.5 （按需）新建一个 Hexo 博客

如果还没有博客目录，先创建一个（只需做一次）：

```powershell
cd D:\Blog
hexo init myblog
cd myblog
npm install
```

之后在软件「设置」里把 **博客本地路径** 指到 `D:\Blog\myblog` 即可。已用别的方式建好博客的，直接指向那个目录就行。

### 3.6 部署还差一步：装 `hexo-deployer-git` 并配置 `_config.yml`

部署走的是 Hexo 官方的 git 部署器。在**博客目录**里执行（只需一次）：

```powershell
cd D:\Blog\myblog
npm install hexo-deployer-git --save
```

然后编辑博客根目录的 `_config.yml`，把最后的 `deploy:` 改成你自己的仓库（缩进必须用空格）：

```yaml
deploy:
  type: git
  repo: https://github.com/你的用户名/你的用户名.github.io.git
  branch: main
```

> **注意**：软件「部署」页里填的「仓库地址 / 分支」只保存在软件本地库里、用于记录部署历史和作为前置检查；**真正推送目标由博客 `_config.yml` 的 `deploy:` 段决定**。两边填一致最省心。

---

## 四、启动与首次使用流程

### 4.1 启动

在项目目录（开发版）：

```powershell
cd E:\HEXO\工具
npm start
```

正式版双击安装后的 `Hexo Blog Manager` 即可。

### 4.2 注册 / 登录

首次打开是登录页，切到「注册」标签：**用户名 + 密码（至少 4 位）+ 确认密码**，注册完即进入。密码用 bcrypt 哈希存储，不明文保存。登录页可勾「记住我」，下次自动登录。

### 4.3 环境检测门

登录后、进主界面前，会自动检测 Node.js / Hexo CLI / Git：

- 全部就绪 → 直接点「进入软件」。
- 缺 Git（有 Node+Hexo）→ 提示「基础已就绪，缺 Git 仅影响部署」，仍可进入。
- 缺 Hexo（有 Node）→ 出现 **「一键安装 Hexo CLI」** 按钮，点它会跑 `npm install -g hexo-cli`，装完自动重新检测。
- 检测失败（权限等）→ 可点「仍要进入」先用着，缺的功能到时再装。

### 4.4 设置博客路径（关键一步）

进软件后，**先去「设置」**：

1. **博客本地路径**：填你博客目录的绝对路径，如 `D:\Blog\myblog`。
2. 站点标题、博客网址（可选，用于仪表盘展示）。
3. 点「保存配置」。

不给路径的话，写文章会只存软件库、预览和部署不可用、首页顶部会持续弹「初始化提醒」。

### 4.5 扫描已有文章（可选）

设置好路径后，去「文章」页点 **「📥 扫描现有文章」**：软件读取博客 `source/_posts/*.md`，解析 front-matter（标题/标签/分类/日期），导入软件库并自动关联到原文件。重名标题会跳过，不覆盖。

之后你在软件里编辑保存，会写回同一个 `.md` 文件，删除则会从 `source/_posts` 删掉该文件。

---

## 五、各模块使用说明

### 5.1 🏠 首页仪表盘

- **三张数据卡片**：已发布 / 草稿 / 待发布 数量（实时）。
- **最近动态**：最近 15 条写文章/部署等操作的时间线，可看时间。
- **站点状态**：博客网址、上次部署时间。
- **快捷操作**：「新建文章」「主题与插件」「部署」三个按钮直达对应页。
- 顶部「初始化提醒条」：环境或博客路径未就绪时出现，给「去设置 / 重新检测 / 稍后提醒」三个动作，就绪后消失。

### 5.2 📄 文章管理

**列表**：表格展示标题 / 状态 / 日期 / 标签 / 操作；顶部搜索框按标题模糊搜，下拉按 `全部/已发布/草稿/待发布` 筛选。

**编辑器**：点「+ 新建文章」或某篇「编辑」打开。从左到右：
- 标题输入框 + 右上 ✕ 关闭（也支持 Esc）。
- **状态**下拉：草稿 / 已发布 / 待发布。
- **标签**、**分类**输入框（逗号分隔，如 `随笔, 技巧`）。
- **格式工具栏**：粗体 `B`、斜体 `I`、`H1`、`H2`、列表、代码块、引用、链接 + 右侧「📂 导入文件」「清空」。
- **双栏**：左边行号 + Markdown 文本区（支持 Tab 缩进），右边实时 HTML 预览，底部字数/字符统计。
- **导入文件**：点「📂 导入文件」选本地 `.txt` 或 `.md`，软件读全文回填正文；若文件含 front-matter，还会自动回填标题/标签/分类。
- **保存**：点「保存」，写库 + 同步写回博客 `source/_posts/<source_file>.md`（未配博客路径则只存软件库并提示原因）。

**操作**：列表里每行有「编辑」；已发布的有「撤回」、未发布的有「发布」一键改状态；「删除」会二次确认后同时删库和删文件。

### 5.3 👁️ 本地预览

- **启动预览**：跑 `hexo server`（默认 4000 端口，可在设置改 `preview_port`），启动后内嵌 iframe 展示博客。
- 按钮状态随运行状态统一：运行中「启动」变灰、「停止」可点、「在浏览器中打开」和「重新载入」可用；未运行则反过来。
- **停止预览**：杀掉 `hexo server` 子进程树。
- **在浏览器中打开**：用系统默认浏览器打开预览地址。
- **没装 Hexo 怎么办**：点「启动预览」时若没检测到 Hexo CLI，状态行会给出 **「一键安装 Hexo CLI」** 按钮，点它跑 `npm i -g hexo-cli`，装完自动再次点启动。装好后就能正常预览。

### 5.4 🎨 主题与插件

顶部显示当前博客路径，未配置则功能不可用并给「去设置」入口。

**主题**：
- 列出博客 `themes/` 目录与 npm 安装的 `hexo-theme-*`，标记「当前」主题。
- 点「启用」切换（只改博客根 `_config.yml` 的 `theme:` 行，不动其它配置）。
- 「安装主题」输入 `butterfly` 或 `hexo-theme-butterfly`，点安装即在博客目录跑 `npm install`。
- 「🌐 浏览在线主题市场」打开 https://hexo.io/themes/ 选主题。
- 「在线预览」跳到对应主题的 npm 页面。

**插件**：
- 列出 `package.json` 里的 `hexo-*` 插件，按类型（部署/生成/渲染/功能/其他）打标签，可卸载（二次确认）。
- 输入插件名如 `hexo-generator-search`，点「安装」即 `npm install`。

### 5.5 🚀 部署

- **配置**：填仓库地址（`repo_url`）、分支（`branch`），保存配置（存软件库，作前置检查 + 历史）。
- **一键部署**：依次跑 `hexo clean → hexo generate → hexo deploy`（`clean` 失败不中断，属正常）。结果与每步日志展示在下方，成功/失败都 toast 提示，时长以毫秒计。
- **部署历史**：表格列出最近 20 次（时间 / 结果 / 仓库 / 用时）。
- **前置条件**（任一不满足会直接报错，不浪费跑半截才发现缺东西）：
  1. 已填仓库地址（软件配置）；
  2. 已配博客本地路径（设置）；
  3. Hexo CLI + Git 已装；
  4. 博客已装 `hexo-deployer-git` 且 `_config.yml` 的 `deploy:` 段指向你要推的仓库（见 3.6）。

  > 配置上传目标若实际推错了仓库，多半是 `_config.yml` 的 `deploy:` 还没改对，软件不替你改这个文件。

### 5.6 ⚙️ 设置

- **外观主题**：一键切亮色/暗色，关闭重开仍保持选择。
- **Hexo 配置**：博客本地路径、站点标题、博客网址，点「保存配置」。
- **环境检测**：点「检测环境」显示 Node.js / Hexo CLI / Git 版本，缺哪个给安装指引文案。
- **数据备份**：导出 `.json`（可读）、导出 `.bak`（专用）、从备份恢复。恢复前会确认，结果会显示恢复前/后条数。

### 5.7 👤 用户主页

- **头像**：点「更换头像」选本地图片（≤8MB），软件自动压缩到 **160px / JPEG 0.9** 存为 data URL。可「移除头像」恢复默认。头像会同步到右上角侧栏。
- **编辑资料**：用户名、显示名称、个人简介，保存生效。
- **修改密码**：输当前密码 + 新密码（≥4 位），结果行有反馈。

---

## 六、数据存储与备份恢复

- 本地 SQLite 库，位置：Electron 用户数据目录下的 `data.sqlite3`（开发版在 `userData`）。
- 表：`posts`（文章）、`settings`（配置/头像/主题等键值）、`deploy_logs`（部署历史）。用户资料存 `users` 表。
- **备份导出**：设置里「导出 .json / .bak」，弹系统保存框自选位置。
- **恢复**：选备份文件，会提示覆盖确认；恢复后显示前后条数。
- 重装软件不会自动删本地库，数据保留。

---

## 七、命令行对照表（从 CLI 迁移过来的人看）

| 软件 GUI 操作 | 对应命令行 |
|------|------|
| 文章 → 新建文章 → 保存 | `hexo new "标题"`（软件用库 + 写文件代替手敲） |
| 文章 → 扫描现有文章 | 读取 `source/_posts/*.md` 并导入库 |
| 本地预览 → 启动预览 | `hexo server`（默认 4000 端口） |
| 本地预览 → 停止预览 | Ctrl+C |
| 主题与插件 → 安装主题 | `npm install hexo-theme-xxx` |
| 主题与插件 → 启用主题 | 改 `_config.yml` 的 `theme:` 行 |
| 主题与插件 → 安装插件 | `npm install hexo-xxx --save` |
| 部署 → 一键部署 | `hexo clean && hexo generate && hexo deploy` |
| 设置 → 环境检测 | `node -v && hexo -v && git --version` |
| 一键安装 Hexo CLI | `npm install -g hexo-cli` |

---

## 八、常见问题

**Q1：换了头像没反应 / 报「无法读取该图片」。**
软件把选中的图片读成 `data:` URL 再压缩存储（已兼容 CSP）。若仍失败，确认图片是常见格式（png/jpg/webp）且 ≤8MB，重试一次。

**Q2：点「启动预览」提示「未检测到 Hexo CLI」。**
旁边有「一键安装 Hexo CLI」按钮，点它装好（需已装 Node.js），装完自动重试启动；或自己在 PowerShell 跑 `npm install -g hexo-cli`。

**Q3：部署点了没反应 / 失败。**
按顺序自查：① 设置里填了仓库地址；② 配了博客本地路径；③ 装了 Hexo + Git；④ 博客目录跑过 `npm install hexo-deployer-git --save`；⑤ `_config.yml` 的 `deploy:` 段 `repo/branch` 正确。步骤日志会显示卡在哪一步。

**Q4：预览按钮有时是灰的。**
按运行状态统一：运行中「启动」灰、「停止」可用；未运行则相反，等状态刷新到一致即可，不用来回猛点。

**Q5：文章保存后软件里有，但博客目录没文件。**
说明没在「设置」配置博客本地路径，文章只存软件库。补上路径后重新编辑保存即可写回 `source/_posts`。

**Q6：备份恢复会不会清掉现有数据？**
会覆盖当前库，恢复前有二次确认，恢复后显示「恢复前 X 条 → 恢复后 Y 条」。

---

## 九、开发与相关文件

- 主进程：`src/main/index.js`（IPC 总线）、`src/main/preload.js`（通道白名单）
- 后端服务：`src/core/`（auth/posts/dashboard/settings/deploy/backup/scanner/themes/preview/env/markdown）
- 前端：`src/renderer/index.html` + `renderer.js` + `styles.css`
- 配套文档：`PRD.md`（需求）、`package.json`（脚本）

```powershell
npm start        # 启动桌面应用
npm test         # 跑单测
npm run dist     # 打包 Windows 安装包（nsis）
```

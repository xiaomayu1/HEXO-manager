# 交接文档：插件配置 + 自定义页面生成

> 生成日期：2026-08-10
> 范围：`E:\HEXO\工具`（Hexo Blog Manager，Electron 桌面应用）
> 目标：解决「装了插件要改 Hexo 代码 / 生成新页面」的痛点，让 GUI 用户不敲 yaml 也能配好插件、建自定义页面。
> 状态：**方案已定，尚未编码** —— 本文档是接手者的执行说明书，照做即可。

---

## 一、问题与现状

### 1.1 痛点
Hexo 生态大量插件 `npm install` 完不能直接用，需要在博客根 `_config.yml` 追加配置段。典型例子：

| 插件 | 装后必须加到 `_config.yml` 的配置 |
|------|-----------|
| `hexo-generator-search` | `search:` 段（`path`/`field`/`content`/`format`） |
| `hexo-generator-searchdb` | 同上 |
| `hexo-abbrlink` | `abbrlink:` 段（`alg`/`rep`），且要让 `permalink: posts/:abbrlink/` |
| `hexo-blog-encrypt` | `encrypt:` 段 + 每篇 front-matter 加 `password` |
| `hexo-filter-nofollow` | `nofollow:` 段（`enable`/`field`/`exclude`） |
| `hexo-tag-canvas` 等 | 主题模板或单页里加特定标签调用 |
| 主题 `hexo-theme-butterfly` | 需 `_config.butterfly.yml`（主题级配置），不写则用默认 |

另有一类需求是「生成一个独立页面」（如关于页、友链页、归档页、自定义 html 落地页），对应 hexo 的 `source/<slug>/index.md`。当前软件完全没有这个入口。

### 1.2 现有代码覆盖到哪一步（已核实）
- `src/core/themes.js` —— **只做**：`npm install` / `npm uninstall --save`；`listThemes`（扫 `themes/` 目录 + `package.json` 的 `hexo-theme-*`）；`activateTheme`（**只把 `_config.yml` 顶格 `theme:` 行改个名**，不动其它字符）。
- `src/core/scanner.js` —— 只读 `source/_posts/*.md`，不动 `source/` 其它部分。
- `src/core/syncHexo.js` —— `writePostFile` / `deletePostFile`，只对 `source/_posts`。
- `src/renderer/renderer.js` `renderThemes`/`loadPlugins` —— 插件表只有「类型/版本/卸载」三列，没有「配置」入口。
- IPC：`themes:list/activate/install`、`plugins:list/install/uninstall` —— **没有任何读/写博客配置文件**的通道。

**结论**：装完插件 → 软件里没人引导用户改 `_config.yml` → 用户只能去手动敲 yaml，正是本工具要消除的终端操作。本方案补这一段。

### 1.3 安全红线（务必遵守）
1. **绝不静默覆盖 `_config.yml`**：每次写前先备份为 `_config.yml.bak`（若已存在则 `_config.yml.bak.2` 递增），让用户能回滚。
2. **绝不删用户的 key**：配方是「合并/补全」语义——只追加缺失的段，已存在的同段当用户已自定义，提示但不覆盖，或弹出 diff 让用户选。
3. **不碰主题模板源码**：本方案不自动改 `themes/<theme>/layout/*.ejs` —— 那会污染主题包、升级即丢。需要改模板的，弹窗里只「给出编辑入口 + 说明」，保存与否由用户决定。
4. **纯增量**：不动现有 install/uninstall/activate 逻辑、不动 `posts/scanner/syncHexo`、不加任何「自动写」的隐式行为。

---

## 二、方案

两条线，可分两期落地；推荐第二期（通用编辑器）作为基础，第一期（配方）构建其上。

### 2.1 第一期：插件一键配置配方 + 自定义页面

**A. 配方库（新文件 `src/core/pluginRecipes.js`）** —— 纯数据，易扩展、易测：
```js
// 每条配方：插件名 -> 需要 merge 进博客 _config.yml 的 yaml 文本 + 注释
const RECIPES = {
  'hexo-generator-search': {
    label: '本地搜索',
    desc: '为博客生成 search.xml，配合主题搜索框使用。',
    config: '\nsearch:\n  path: search.xml\n  field: post\n  content: true\n  format: html\n',
    note: '会在博客根 _config.yml 追加 search: 段。'
  },
  'hexo-abbrlink': {
    label: '永久链接',
    desc: '文章生成固定 abbrlink，避免改链接后失效。',
    config: '\nabbrlink:\n  alg: crc32\n  rep: hex\n',
    permalink: 'posts/:abbrlink/',
    note: '建议把 _config.yml 的 permalink 改为 posts/:abbrlink/，否则插件不生效。'
  },
  'hexo-filter-nofollow': {
    label: 'SEO 外链',
    desc: '给外链加 rel="nofollow"，提升站内权重。',
    config: '\nnofollow:\n  enable: true\n  field: site\n  exclude: "example.com,github.com"\n'
  }
};
module.exports = { RECIPES };
```
- 仅本地 json/key-value，无副作用，可直接单测。
- 未收录插件 → 编辑器路径兜底（见 2.2），不强求覆盖全生态。

**B. 自定义页面（在已装 Hexo 前提下）** —— 复用博客 `source/<slug>/index.md` 约定：
```
source/about/index.md   —— 生成 <博客>/about/index.html
source/friends/index.md —— 生成 <博客>/friends/index.html
```
- 弹窗填「别名 slug（如 about）/ 标题 / 是否加密 / 正文 md」→ 写文件。
- 「是否在导航显示」：Hexo 主题导航多读 `_config.yml` 的 `menu:` 段，给一个 checkbox「同时把该页加到 _config.yml 的 menu: 段」，勾上则合并一行 `about: /about/`。

**C. 渲染层入口**：在 `renderThemes` 的插件表「操作」列加一个「配置」按钮（仅当插件在 RECIPES 里时显示，否则文案改「编辑配置」走通用编辑器）：
```
[卸载] [配置]   // 已收录
[卸载] [编辑配置]  // 未收录
```
顶部「主题与插件」区另加一个醒目按钮 `＋ 新建自定义页面`，进页面生成弹窗。

### 2.2 第二期（推荐）：通用 `_config.yml` 编辑器
- 弹窗分两栏：左只读高亮展示博客 `_config.yml` 全文；右可编辑文本框（带 yaml 行号），保存前自动备份 `.bak`。
- 可选「主题配置」Tab：读 `_config.<theme>.yml`（若存在），同样备份 + 编辑。
- 这是配方的安全网：配方没收录、或用户要微调密钥，都能在这里改。
- 这条线**独立于 **第一期**，可单独上线，一期随后在其基础上做"配方 → 一键写入"按钮。

---

## 三、落到哪些文件（已核实锚点）

下表所有"锚点"在本次起草时均已用脚本逐条验证 `split(find).length-1 === 1`，可直接 `String.replace` 命中。

### 3.1 新增文件
- `src/core/pluginRecipes.js` —— 见 2.1A，纯数据。
- `src/core/blogConfig.js`（可选，也可直接放 themes.js 内） —— `_config.yml` 的读/写 + ——尝试合并段。建议：`readConfig(blogPath)`、`writeConfigWithBackup(blogPath, newContent)`、`mergeYamlSegment(existingContent, segmentText)` 三个纯函数，注入 `fs` 便于测。

### 3.2 `src/core/themes.js`（追加方法，不动现有）
在 `createThemesService` 返回的对象里追加：
```js
function readBlogConfig(blogPath) {
  requireBlogPath(blogPath);
  return fsx.readFileSync(path.join(blogPath, '_config.yml'), 'utf8');
}
function writeBlogConfig(blogPath, content) {
  requireBlogPath(blogPath);
  const p = path.join(blogPath, '_config.yml');
  // 备份递增（adv 2.4 顺序写好 —— 若 .bak 存在则 .bak.2 .bak.3）
  backupIncremental(fsx, p + '.bak');
  fsx.writeFileSync(p, content, 'utf8');
  return { ok: true };
}
function mergeRecipe(blogPath, recipeConfig) { /* mergeYamlSegment -> writeBlogConfig */ }
```
- 注意 themes.js 的 `fsx` 已可注入（`opts.fs`），新增方法沿用之；`pluginCategory` 已存在可复用。

### 3.3 `src/core/app.js`
在返回对象里把新方法带出（**只增 key，不删**）：
```js
return { ..., themes, ..., /** 新增：**/ blogConfig: themes /* 复用，或单独 */
};
```
> 推荐：把 blogConfig 的方法挂在 themes 上（不新加 service），减少跨模块耦合；或仿照 preview 单建 `createBlogConfigService(fs)`。

### 3.4 `src/main/index.js`（追加 IPC，锚点在 `plugins:uninstall` 之后）
锚点：`handle('plugins:uninstall', (pkg) => a.themes.uninstallPlugin(blogPathOf(), pkg));` —— 之后插入：
```js
// 通用配置读写 + 备份
handle('config:read', () => { try { return { ok: true, text: a.themes.readBlogConfig(blogPathOf()) }; } catch(e){ return { ok:false, error:(e&&e.message)||'读取失败' }; } });
handle('config:write', (text) => a.themes.writeBlogConfig(blogPathOf(), String(text||'')));
// 配方：一键合并某插件配置
handle('plugins:applyRecipe', (pkg) => { try { return a.themes.mergeRecipe(blogPathOf(), recipeFor(pkg)); } catch(e){ return {ok:false,error}(e&&e.message)||'应用失败'}; });
// 新建自定义页面：source/<slug>/index.md（含可选 menu 项）
handle('pages:create', (info) => { try { return pagesCreate(blogPathOf(), info); } catch(e){ return {ok:false,error}; } });
```
- `recipeFor(pkg)` 查 `pluginRecipes.RECIPES`，未收录返回 `{ok:false, error:'该插件暂无内置配方，请用「编辑配置」手动添加'}`。
- `pagesCreate` 也建议作纯函数放在 `src/core/pages.js`，注入 `fs`。

### 3.5 `src/main/preload.js`（追加白名单，锚点在 `plugins:uninstall` 行后）
```
'themes:list','themes:activate','themes:install',
'plugins:list','plugins:install','plugins:uninstall',
'config:read','config:write','plugins:applyRecipe','pages:create',   // 新增
```
`request_user_input`/`on` 通道：本方案都是 `invoke`，无需新增 `on`。

### 3.6 `src/renderer/renderer.js` `loadPlugins`（追加按钮列）
锚点：插件表 row 模板里那行 `data-pl`。把 `<td class="row-actions">` 改成两条按钮，且对在 RECIPES 的插件多显示「配置」：
```js
const hasRecipe = RECIPES[p.name];  // 渲染端从仍是调 IPC 取，或加 `plugins:list` 返回里带 `hasRecipe`
const actions = hasRecipe
  ? `<button class="btn" data-pl="${esc(p.name)}" data-act="uninstall">卸载</button><button class="btn primary" data-pl="${esc(p.name)}" data-act="config">配置</button>`
  : `<button class="btn danger" data-pl="${esc(p.name)}" data-act="uninstall">卸载</button><button class="btn" data-pl="${esc(p.name)}" data-act="edit">编辑配置</button>`;
```
- 点击「配置」/「编辑配置」都打开同一个弹窗（见 3.7），只是初始内容不同：前者把配方段拼进右编辑框待用户审；后者打开通用编辑器。
- 已核实：`loadPlugins` 闭包里有 `confirmModal` / `toast`，可直接复用。

### 3.7 弹窗 UI（新加一个 renderer 函数 `openConfigModal`）
- 复用现有 `.modal`/`.modal-card`（`renderer.js:13` `confirmModal` 同款），但比 modal 大：建议新建 `.config-modal-card { width:min(720px,94vw); max-height:86vh; display:flex;flex-direction:column; }` 类，避免改原 `.modal-card`。
- 头部：插件名 + 简短描述；中间两栏（左只读 diff/右可编辑）；底部 `取消 / 恢复备份 / 保存`。
- 「保存」调 `config:write`，成功后 `toast('已保存 _config.yml（原文件备份为 .bak）', 'ok')`。
- 已核实可用：`confirmModal`、`toast`、`esc`、`$`、`api.invoke`。

### 3.8 `src/renderer/styles.css`（文末追加，沿用 CSS 变量）
- `.config-modal-card`：见 3.7。
- `.config-editor`（右栏 textarea）：等宽、行号、亮暗适配，复用 `.preview pre` 的色调。
- `.recipe-note`：用 `--warn` 着色提示"会修改 _config.yml，已自动备份"。

### 3.9 单测（建议，不强制）
- `tests/pluginRecipes.test.js`：验证索引存在、`permalink` 等字段格式。
- `tests/blogConfig.test.js`：用注入的 fake fs 验证 `mergeYamlSegment` 不破坏已有段、备份递增编号。
- 风格仿 `tests/themes.test.js`（已有）。

---

## 四、安全与边界

- 写 `_config.yml` **永远**先备份；备份失败（磁盘满/只读）则**不写**并报错。
- 合并段时若检测到同 key 已存在，**不静默覆盖**：默认跳过 + 在弹窗里标红"该段已存在，已跳过"，用户可在通用编辑器里手动改。
- 自定义页面若 `source/<slug>/index.md` 已存在：弹窗确认「覆盖 / 取消」，不允许静默覆盖。
- 触发一切前都走 `blogPathOf()`；未配博客路径时所有新 IPC 返回 `{ok:false, error:'尚未配置博客本地路径'}`（与 themes.js `requireBlogPath` 一致）。
- 不动 `hexo-deployer-git`、`hexo-renderer-*` 等核心插件配置 —— 原则上配方只针对"非核心、装了易漏配"的插件。

---

## 五、验证清单（接手者做完后自查）

1. 装一个 `hexo-generator-search` → 在插件列表点「配置」→ 弹窗里看到 `search:` 段 → 保存 → 博客根出现 `_config.yml.bak`、`_config.yml` 里多了 `search:` 段。
2. 在通用编辑器里改 `_config.yml` 的 `site.title` → 保存 → 重启 hexo server 看到标题变化。
3. 点「新建自定义页面」填 `slug=about` → 生成 `source/about/index.md` → 勾「加到导航」→ `_config.yml` 的 `menu:` 多一行。
4. 二次覆盖：把 `_config.yml.bak` 改名 `_config.yml`，启动软件应能读不报错。
5. 卸载插件依旧走原 `plugins:uninstall`，行为不变。
6. `npm test` 全绿；新测试若加了补在主题相关套件旁边即可（不强制改其他用例）。

---

## 六、已核实的事实清单（给接手者省力）

- `themes.js` 模块级有 `fs`、`path`、`pluginCategory`、`createThemesService`，**已导出** `parseActiveTheme/applyThemeLine/pluginCategory/listNpmPlugins/listNpmThemes`，新方法追加在 `createThemesService` 返回对象里即可被 `app.js`/`main` 取到。
- `app.js` 第 46 行返回对象 `themes` 即 `createThemesService(opts.themes || {})`，无 fs 注入——若要测，可仿照 scanner/themes 在 main 里传 `a.themes`，或新加 `createBlogConfigService({fs, blogPathProvider})`。
- `main/index.js` 已 `const fs = require('fs')`、`const path = require('path')`、`const { spawnSync, spawn } = require('child_process')`，无需再加。
- `blogPathOf()` 已定义为 `backend.settings.getBlogPath()`，所有新 handler 照用即可。
- `preload.js` invoke 白名单按"主题与插件"那一段线性排列，新通道就接在后面，避免到处散落。
- `renderer.js`：`$`/`$$`/`esc`/`toast`/`confirmModal`/`api.invoke` 全局可用，无需补依赖；`renderThemes` 在 `line 509` 起，`loadPlugins` 紧随其后。
- CSS 变量名（已核实存在）：`--panel`/`--panel-2`/`--border`/`--text`/`--muted`/`--warn`/`--accent-grad`/`--radius`/`--shadow`，新样式全部沿用，确保亮暗主题自动适配。
- 现有 `.hidden { display:none !important; }` 可直接用来切显隐。
- 不存在 `config:*` / `pages:*` 通道名，无冲突。

---

## 七、与上一轮"菜单删除"无关，但需注意的并行任务快照

- 顶部默认菜单栏已由 `Menu.setApplicationMenu(null)` 移除（main/index.js `app.whenReady`）。
- 文章编辑器已改为浮层弹窗（`editor-modal-overlay`）。
- 预览页有 F11 全屏 + 浮层工具栏 + ready 轮询（main `preview:setF11Hook`）。
- 启动有平台警告弹窗 + 翻页查 `使用手册.md`（`notice:manual` 通道）。
- 注册页有注册提示。

这些与本交接文档范围**不冲突**，新文件/新 IPC 通道名都已避开。

---

## 八、起步建议（最小可演示版）

1. 先写 `src/core/pluginRecipes.js` + `themes.js` 的 `readBlogConfig/writeBlogConfig/mergeRecipe` 三个方法 + 对应单测（用注入 fs）。
2. 加 2 个 IPC：`config:read` / `plugins:applyRecipe` + 白名单。
3. 渲染层：先做最简单的「配置」按钮 → 调 `plugins:applyRecipe` → 显示**将要写入的 diff 文本** to toast，确认后再写。**先不做通用编辑器 UI**，能演示"装完一键配 search"就走通了。
4. 验证清单跑 1、5 两项即可对外宣称 demo。
5. 通用编辑器与自定义页面再做二期。

—— END ——
